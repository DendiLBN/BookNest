# E2E tests

This folder is reserved for browser-level end-to-end tests, for example with Playwright.

Current frontend tests are component and hook tests under `src/__tests__` and run with Vitest.
E2E tests should live here so they are not mixed with unit/component tests.
